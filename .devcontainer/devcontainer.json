{
  "name": "Kuping Negara MLOps",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "eamodio.gitlens"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${containerWorkspaceFolder}/.venv/bin/python",
        "python.testing.pytestEnabled": true,
        "python.testing.pytestArgs": [
          "tests"
        ]
      }
    }
  },
  "postCreateCommand": "python -m pip install --disable-pip-version-check uv==0.12.12 && uv sync --frozen --extra dev && uv pip check && uv run --frozen --extra dev python -m kuping_negara.healthcheck",
  "remoteUser": "vscode"
}
